Version 1.3.0 (2026-04-19)
--------------------------
Watched-team scoring alerts with full-viewport Sweep Reveal animation

NEW FEATURES:
+ Watched Teams — full-viewport scoring alert system
  - New "Alerts" tab in Settings lets you select any of the 30 MLB teams to watch
  - When a watched team scores (or an opponent scores against them), the scrolling ticker
    freezes and a full-viewport alert animates in
  - Alert text auto-fits the bar width using Ozone font: "YANKEES SCORE  Judge: Home Run (2 RBI)"
  - Animation uses Sweep Reveal: a gold scanner beam sweeps right→left, revealing bright gold
    text in its wake against a translucent team-colour background with a dark top vignette
  - After the reveal, text pulses gently (±7.5% brightness sine wave) during the hold phase
  - Alert fades out smoothly then the ticker immediately resumes scrolling
  - Alert duration configurable from 3–15 seconds (default 6 s)
  - Separate toggles: alert when your team scores vs. alert when opponent scores against your team
  - Multiple alerts queue up and play in order (300 ms gap between each)
  - Alerts queued during the intro animation start 400 ms after the intro finishes
  - De-duplication via atBatIndex prevents the same scoring play from triggering twice
    across multiple 10-second data-poll cycles
+ Game-start alerts — "GAME STARTING  YANKEES vs METS"
  - Fires once per game when status transitions from Scheduled/Pre-Game/Warmup → In Progress
  - Uses home team colour; if only the away team is watched, uses away team colour
  - If both teams are watched, home team colour takes precedence
  - Toggle in Settings → Alerts: "Alert when watched team's game starts"
+ Game-final alerts — "FINAL  YANKEES DEFEAT METS  10-1"
  - Fires once per game when status transitions to Final/Completed/Game Over
  - Always uses the winning team's colour regardless of which team is watched
  - Tie results render as "FINAL  YANKEES 3-3 METS (TIE)"
  - Toggle in Settings → Alerts: "Alert when watched team's game finishes"
+ Delayed Start / Delayed games now appear in the ticker
  - Games with status "Delayed Start" or "Delayed" are shown alongside scheduled games
  - Centre element toggles every 2 seconds between the original start time (cyan) and
    the word "DELAYED" (amber #FF8C00)
  - Centre column width is fixed to the wider of the two labels so no layout shift occurs
+ ESPN logo fallback in team logo lookup
  - Added MLB_ESPN_ABBR dict mapping all 30 team nicknames to ESPN CDN abbreviations
  - If a logo file is not found under the standard nickname, the lookup retries using
    the ESPN abbreviation (e.g. "mil.png" for Brewers, "tb.png" for Rays)
+ Hits (H) stat added to live pitcher detail line
  - Pitcher line now shows: IP  H  K  BB  ER

IMPROVEMENTS:
+ Uniform card layout across all game types (in-progress, scheduled, delayed, postponed, final)
  - All cards now use identical block widths and gaps so team names, logos, odds, and
    records sit at the same horizontal positions regardless of game state
  - 2-row subtext layout uses the same vertical baselines as the 3-row live layout
  - Delayed and Postponed games now show probable pitcher subtext
+ Player info rows shifted up for better vertical centering within the ticker bar
  - Top subtext row moved up 4 px; bottom live-stats row moved up 3 px (net)
+ Hotkeys updated
  - I  = Restart Intro animation  (previously R)
  - R  = Replay last alert  (re-queues and re-plays the most recently shown alert)
+ Settings → Hotkeys tab updated to reflect new I / R assignments

IDLE POLLING:
+ Idle polling now exits immediately when any game enters Warmup or Pre-Game status
  - Previously relied solely on a 2-minute countdown before first pitch
  - Games in Warmup/Pre-Game are now treated as imminent, switching to the normal
    user-configured update interval instantly rather than waiting up to 5 minutes


Version 1.2.0 (2026-04-16)
--------------------------
Major update with score animations, yesterday mode improvements, pause stability, and automated versioning

NEW FEATURES:
+ Score changes now glow gold for 2.5 seconds
  - When a score updates, the number changes color from white to gold (#FFD700)
  - After 2.5 seconds, the color fades back to white
  - Each team's score glows independently (away and home tracked separately)
  - Automatic cache invalidation ensures smooth color transitions
  - Uses high-precision timer for accurate glow duration
+ Yesterday mode now shows ALL games including in-progress
  - Previously filtered to only Final/Completed/Game Over statuses
  - Now includes any west coast late games still in progress
  - Periodic fetches continue during yesterday mode to update in-progress games
  - Smart polling applies (live games get frequent updates, finals get idle polling)
+ Build script (build_performance.bat) now automatically extracts version from MLB-TCKR.py
  - Reads VERSION constant from MLB-TCKR.py line 13
  - Automatically updates version-mlb-tckr.txt with 4-digit version format
  - Both .spec files now reference version-mlb-tckr.txt for consistent version embedding
  - Displays "Building version x.x.x" during build process

IMPROVEMENTS:
+ Ticker speed maximum increased from 16 to 30
  - Settings dialog slider now allows values 1-30
  - Keyboard + shortcut can now increase speed up to 30
  - Tooltip updated to reflect new range
+ LOADING overlay now only appears during significant state changes
  - Shows on initial app startup, when switching between days (Yesterday/Today/Tomorrow),
    or when recovering from a DATA DELAYED event
  - Regular intra-day periodic fetches no longer trigger the LOADING indicator
  - Reduces visual noise during normal operation
  - start_data_fetch() now accepts optional show_loading parameter for explicit control
  - _first_fetch flag tracks whether initial startup fetch has completed
+ In-progress games now show pitcher info in "P: Name ERA W-L" format (same as pre-game)
  - Previously showed "LastName ERA" with no "P:" prefix and no W-L record
  - Season W-L pulled from the existing boxscore players_map — no extra API call
+ Warmup-status games now render probable pitcher subtexts in the ticker
  - 'Warmup' added to scheduled_subtext_enabled status set

FIXES:
* Fixed ticker jumping/shifting position when paused and data updates
  - Root cause: when build_ticker_pixmap() rebuilt the ticker due to score/count changes,
    _scroll_max_width could change slightly (due to text width variations), causing the
    content at scroll_offset to shift visually even though the numeric offset stayed constant
  - Fix: scroll position is now preserved as a ratio (scroll_offset / _scroll_max_width)
    before any pixmap rebuild while paused, then restored afterward
  - Applies to all rebuild triggers: game data updates, odds updates, settings changes,
    monitor switches, and error recovery
  - Also prevents _last_frame_ms resets when paused, avoiding time jumps on unpause
* Fixed yesterday mode filtering out in-progress games
  - _effective_fetch_date() now returns yesterday's date when in automatic yesterday mode
  - Ensures all games are shown, not just completed ones


Version 2.7.3 (2026-04-14)
--------------------------
DPR-adaptive font antialiasing

FIXES:
* Fixed Ozone font rendering as "jagged/blocky" on standard-DPI (1× DPR) displays
  - Root cause: NoAntialias | NoSubpixelAntialias was hardcoded for all fonts
    regardless of display DPI. On a 4K/HiDPI display (DPR 2×) this is correct —
    enough physical pixels exist to render the LED circles crisply without AA.
    On a standard 1× DPR display, AA is needed or the circles can only be
    approximated with a handful of aliased pixels and look jagged.
  - Fix: font style strategy and painter TextAntialiasing hint are now selected
    at startup based on self.dpr:
      DPR ≥ 2.0 → NoAntialias | NoSubpixelAntialias | ForceIntegerMetrics (unchanged)
      DPR < 2.0 → PreferAntialias | ForceIntegerMetrics (AA enabled)
  - Stored as self._font_style_strategy and self._text_aa_hint; applied to all
    fonts (_qfont, small_font, time_font, vs_font, tiny_font) and all painters
  - Also updated when settings are changed or ticker is moved to another monitor
    via keyboard shortcut (1–4)

Version 2.7.2 (2026-04-14)
--------------------------
Display logic, W-L/moneyline layout overhaul, startup flash fix

NEW FEATURES:
+ W-L records now shown on yesterday's final games (previously only on live/scheduled)
+ Probable pitcher placeholder — scheduled games with no pitcher listed now show "P: -"
  instead of blank player info

IMPROVEMENTS:
+ W-L and moneyline positions swapped for all game types
  - W-L record is now on top (aligned with team name cap height)
  - Moneyline is now on the same baseline as the team name (bottom row of the column)
  - Applies to live, final/yesterday, scheduled games, away and home sides
+ Moneyline text moved up to team name baseline, eliminating crowding against player info
+ "Show Player Names, Record" setting now fully applies to all game states
  - When disabled: W-L, player names (pitchers, WP/LP, batter info) all hidden
  - Applies to today, tomorrow, and yesterday's games
  - Layout reverts to single-row (team name vertically centered) when disabled
+ Side column (W-L / moneyline) now appears for yesterday finals even when moneyline
  is disabled — block widths expand to fit the W-L text

FIXES:
* Fixed startup flash of today's pre-game schedule before yesterday's finals appear
  - Root cause 1: 'Preview' status not included in _not_started set, causing a game
    with that status to abort the all-pregame check and show today's schedule
  - Root cause 2: update_timer continued firing during the two-step async fetch,
    and on the second call _pending_today_games was already set, bypassing the guard
    and falling through to "Normal path: display today's games"
  - Fix: stop update_timer immediately when entering wait-for-yesterday state; add
    explicit guard to refresh pending data and return if already waiting
  - Fix: add 'Preview' to _not_started set
* Fixed yesterday fetch error leaving ticker blank — now falls back to displaying
  today's pre-game schedule instead of showing nothing
* Removed stale [SCHED] debug print statements

Version 2.7.1 (2026-04-14)
--------------------------
Scroll judder fix + GPU texture overflow fix + debug cleanup

FIXES:
* Fixed ~1 Hz scroll judder on 60 Hz displays
  - Root cause: timer interval was round(1000/60) = 17 ms, but VBlank period is
    16.667 ms — the ~0.33 ms mismatch accumulates to one missed VBlank per second,
    causing a visible stutter (~1 Hz beat frequency)
  - Fix: timer interval reduced to 8 ms so at least two callbacks fire per VBlank;
    Qt coalesces the update() calls into one paintEvent per VBlank
  - Delta-time scrolling means firing 2× faster has zero effect on scroll speed
  - Applies for all display refresh rates up to 120 Hz

* Fixed GPU texture limit overflow on 4K / HiDPI displays (DPR ≥ 2)
  - With 15+ games at DPR 2.0, the pre-baked ticker pixmap was 80,328 physical
    pixels wide — exceeding GPU max texture dimensions (~16–32 K px), causing
    visual truncation and rendering artefacts
  - Fix: replaced single mega-pixmap with a tile list (_ticker_tiles); paintEvent
    now draws only the tiles that intersect the visible window per frame, repeating
    one period's worth of tiles to fill the screen
  - Per-tile GPU upload is ~2,500 px — well within any GPU limit
  - self.ticker_pixmap = True used as truthy sentinel; all existing boolean guards
    continue to work unchanged

IMPROVEMENTS:
+ Removed synchronous repaint() call in start_data_fetch; replaced with update()
  so the LOADING badge is shown via the normal coalesced paint path rather than
  forcing an extra synchronous paint call on the main thread

Version 2.7.0 (2026-04-13)
--------------------------
Scheduled game layout redesign + probable pitchers + startup improvements

NEW FEATURES:
+ Probable starting pitchers now displayed for scheduled games
  - Shows pitcher name, ERA, and Win-Loss record (e.g., "P: Kershaw, 2.45 8-2")
  - Fetches stats from MLB Stats API using person endpoint with hydrate parameter
  - Displays below team names in small font
  - Falls back to "P: Name, -.-- -" format if stats are unavailable (consistent formatting)

IMPROVEMENTS:
+ Player information layout improvements
  - Away team player info (probable pitchers, live stats, yesterday's stats) is now right-justified
    within the away team block, ending close to the away team logo without touching
  - Home team player info is now left-justified within the home team block, starting close
    to the home team logo without touching
  - Applies to all game states: scheduled (probable pitchers), live (current pitcher/batter),
    and final/yesterday (WP/LP or final stats)
  - Creates visual balance and keeps player names close to their team logos for clarity

+ Scheduled game layout completely redesigned for improved information density
  - Game start times now positioned ABOVE the "vs" text (previously below)
  - W-L records now vertically stacked with moneyline odds in left/right columns
  - Away team: odds and W-L in left column, team name to the right
  - Home team: team name on left, odds and W-L stacked in right column
  - Reduced gap between game time and "vs" text from 3px to 1px for tighter layout

+ Live and final games now show W-L records when odds are displayed
  - W-L records appear below moneyline odds (vertically stacked)
  - Maintains existing player info display (current pitcher/batter for live, WP/LP for final)

+ Startup behavior improvements
  - Ticker now determines whether to show today or yesterday BEFORE displaying games
  - Eliminates brief flash of today's games before switching to yesterday
  - "LOADING" indicator (blue badge, top-right) now shown during all data fetches
  - Loading indicator appears when switching dates via keyboard shortcuts (Y/D/T)
  - Loading badge automatically clears when data arrives or on error

OPTIMIZATIONS:
+ Probable pitcher fetching optimized and parallelized
  - Reduced from 2-3 API calls per scheduled game to 1 call per game
  - Uses game feed API to get probable pitcher IDs, then person API for stats
  - Eliminated attempt to parse schedule API (which doesn't include detailed pitcher data)
  - All game feed API calls now executed in parallel using ThreadPoolExecutor
  - All pitcher stats API calls now executed in parallel using ThreadPoolExecutor
  - Results in 3-5x faster data loading for games with probable pitchers
  - Grouped log messages confirm parallel execution (all "Away probable" / "Home probable" messages appear together)

FIXES:
* Fixed probable pitchers not rendering despite being fetched
  - Subtext now enabled for both live/final games AND scheduled games
  - Layout positioning (record_y) now calculated when probable pitchers are present
  - Scheduled games with probable pitchers properly allocate vertical space

* Fixed startup data display logic
  - No longer displays today's pre-game schedule before fetching yesterday's finals
  - Yesterday fetch triggered immediately if detected on startup
  - Prevents unnecessary ticker rebuild and eliminates visual flash


Version 2.6.1 (2026-04-07)
--------------------------
"Today's Games" pin fix + league SVG images moved to MLB-TCKR.images

FIXES:
* Pressing D to show Today's Games no longer reverts to automatic mode on the next
  periodic fetch.  The override is now stored as "today" instead of None so that
  on_data_received bypasses the yesterday-mode / auto logic and keeps today's games
  pinned until the user explicitly deselects the option or restarts the program.
* Pressing D a second time (while already pinned to today) now correctly returns to
  automatic (auto-yesterday) mode — acts as a proper toggle.

IMPROVEMENTS:
+ american.svg and national.svg (league logos used in the Standings window) are now
  stored in and searched for in the MLB-TCKR.images folder inside %APPDATA%\MLB-TCKR,
  alongside all other team logo images — they are no longer placed in the root
  directory of the bundle or project folder.
+ Both .spec files updated accordingly; the SVG files are bundled into
  MLB-TCKR.images\ rather than the executable root.
+ Search order for league logos: MLB-TCKR.images (AppData) → AppData root →
  MLB-TCKR.images (_MEIPASS) → _MEIPASS root → project folder (backward-compatible).


Version 2.6.0 (2026-04-04)
--------------------------
Show Yesterday / Today / Tomorrow games on demand + keyboard shortcuts

NEW FEATURES:
+ Keyboard shortcuts (active when ticker has focus) — session-only, nothing is saved to settings
  - +       : Increase scroll speed by 1 (max 16)
  - -       : Decrease scroll speed by 1 (min 1)
  - Y       : Show Yesterday's games
  - D       : Show toDay's games (return to auto mode)
  - T       : Show Tomorrow's games
  - R       : Restart ticker (replay intro animation)
  - G       : Refresh / fetch latest game data
  - F       : Toggle FPS counter overlay on/off
  - 1–4     : Move ticker to that monitor number; out-of-range numbers are silently ignored
  - P       : Pause / unpause scrolling (existing)
  - S       : Open Standings window (existing)
  - Q       : Quit (existing)
  - .       : Open Settings dialog (existing)

NEW FEATURES:
+ "Show Games For..." submenu in both the right-click context menu and the system tray icon menu
  - Three checkable options: Yesterday's Games, Today's Games, Tomorrow's Games
  - Selecting an option immediately re-fetches and displays games for that date
  - Clicking the already-active option unchecks it and returns to automatic (today) mode
  - Session-only: the selected date is not saved to settings and resets on restart

+ Date badge overlay on the ticker bar
  - A gold "YESTERDAY" or "TOMORROW" badge is displayed in the top-right corner of the ticker
  - Badge is hidden when displaying today's games (normal mode)

+ Seamless interaction with automatic yesterday mode
  - Switching to a manual date override automatically exits the auto-yesterday mode
  - Clears pending schedule and stops the next-day transition timer


Version 2.5.9 (2026-04-02)
--------------------------
Scrolling, odds, and postponed game fixes

IMPROVEMENTS:
+ Scrolling animation is now perfectly smooth on all displays
  - Scroll timer interval now matches your monitor's refresh rate (Hz)
  - FPS overlay now shows true displayed frame rate, not timer callback rate
  - Eliminates judder caused by timer/VSync mismatch

+ Postponed games are now always shown
  - Status text displays "PPD" instead of a start time for postponed games
  - Postponed games are classified as scheduled (not live or final)

FIXES:
* Moneyline odds are no longer shown for completed/final games
  - Prevents stale odds from appearing on yesterday's finished games
  - Odds only display for scheduled and live games


Version 2.5.8 (2026-03-28)
--------------------------
Performance and STANDINGS visual improvements

IMPROVEMENTS:
+ Speed slider maximum increased from 10 to 16
  - Allows faster ticker scrolling for users who prefer higher speeds
  - Tooltip updated to reflect new range: "1 = slowest, 16 = fastest"

+ STANDINGS window font consistency improvements
  - Division titles now display in uppercase (EAST, CENTRAL, WEST) for better visual hierarchy
  - Column headers (Team, W-L, Pct., L10) now use Ozone font to match division titles and league headers
  - All data fonts changed from PixelFont7-G02A to Gotham Black for consistency with player info font
  - Simplified font loading logic by removing custom .ttf file dependency for standings

+ Font system optimization
  - Removed ~25 lines of file-loading code from load_record_font_family()
  - Now uses system font (Gotham Black) instead of bundled PixelFont7-G02A.ttf
  - Reduces potential failure points in PyInstaller builds

FIXES:
* Restart functionality crash in PyInstaller .exe build
  - Context menu "Restart" option now works correctly in compiled .exe
  - Settings dialog automatic restart when changing ticker height or display monitor now works correctly
  - Fixed sys.executable path handling for frozen applications (both manual and automatic restart)


Version 2.5.7 (2026-03-27)
--------------------------
Font rendering, centering, scaling, and opacity slider improvements

IMPROVEMENTS:
+ Pixel-perfect font rendering for dot-matrix/LED fonts using QFontMetrics.boundingRect for true visual centering; fonts are now vertically centered and no longer clipped or too high.
+ Unified font scaling logic: font size scaling is now consistent at startup and after settings changes (scaling factor unified to 0.40).
+ Opacity sliders in the settings dialog now use a 0–100% (percent) scale instead of 0–255, with updated labels and tooltips for clarity.
  - Settings logic converts opacity values to/from 0–255 for storage and rendering, ensuring backward compatibility.
  - UI is more intuitive and user-friendly for adjusting opacity.

FIXES:
* Font scaling bug at startup resolved; font size now matches settings immediately on launch.
* Font centering and rendering issues fixed for all supported fonts.
* Opacity slider UI and settings logic now match, preventing confusion and ensuring correct opacity application.

MLB-TCKR Changelog
==================

Version 2.5.6 (2026-03-26)
--------------------------
Live settings apply, restart prompt for height/monitor changes, bug fixes

FEATURES:
+ Settings dialog — Apply button now applies hotswappable settings immediately
  without closing the dialog
  - Shared _collect_settings() helper saves to disk and updates in-memory state
  - apply_settings() (Apply button) and save_and_close() (OK button) both call
    it, then invoke apply_live_settings() on the main window
  - Previously the OK button required a manual restart for any setting to take
    effect; now fonts, speed, colors, opacity, LED/glass mode, and record
    display update instantly

+ Restart prompt for settings that require a full restart
  - Changing ticker_height or monitor_index now shows a Yes/No dialog:
      "Ticker height and/or monitor changes require a restart to take effect.
       Restart now?"
  - On Yes the app relaunches itself immediately via subprocess.Popen and then
    quits cleanly via QApplication.quit(); works for both regular Python and
    PyInstaller .exe (detects sys.frozen)
  - On No the setting is saved and takes effect next time the app is opened

FIXES:
* apply_live_settings() / keyPressEvent() methods were accidentally merged —
  keyPressEvent body was appended inside apply_live_settings() after
  self.update() with no def line, causing NameError: name 'event' is not
  defined whenever a hotkey was pressed
  - Fixed by restoring the missing def keyPressEvent(self, event): line


Version 2.5.5 (2026-03-25)
--------------------------
Scroll judder eliminated — glass-smooth motion at all speeds

IMPROVEMENTS:
* Completely eliminated the constant ~2.5 Hz scroll judder visible at all speeds
  and the irregular position jumps more noticeable at higher speeds.
  Four compounding root causes were identified and fixed:

  1. Sub-pixel quantization stutter (the primary ~2.5 Hz pattern)
     - At speed=2 the ticker advanced ~0.96 px/frame; floor(0.96)=0, so the
       ticker held the same pixel column for two consecutive frames then jumped.
       This repeated every 25 frames (~400 ms = 2.5 Hz) exactly matching what
       was observed.
     - Fixed by drawing with QPointF and physical-pixel quantization:
       phys_x = round(offset * dpr) / dpr
       This snaps to the nearest physical pixel (0.5 logical-px steps at DPR 2)
       with zero bilinear filtering or per-frame sharpness variation.

  2. Timer-callback lag
     - Scroll position was computed when the QTimer fired, but paintEvent ran
       some milliseconds later. A busy event loop caused late ticks to produce
       double-sized position jumps on the next frame.
     - Fixed by moving delta-time position computation into paintEvent itself,
       so the displayed position is accurate to the exact moment of rendering.

  3. Integer millisecond precision (QElapsedTimer::elapsed())
     - elapsed() returns qint64 integer ms giving ±1 ms (~6% error at 16 ms
       intervals), causing alternating short/long deltas each frame.
     - Fixed by switching to nsecsElapsed() / 1_000_000.0 everywhere for
       sub-microsecond float-ms precision.

  4. Windows system timer resolution (15.6 ms default)
     - Even with Qt.PreciseTimer, the OS clock granularity meant the 8 ms
       timer fired at wildly inconsistent intervals.
     - Fixed by calling timeBeginPeriod(1) at startup (ctypes, already imported)
       to set the Windows multimedia timer resolution to 1 ms. The OS restores
       the previous resolution automatically when the process exits.

* Timer interval reduced from 16 ms to 8 ms (~120 Hz tick rate)
  - At 16 ms, a single late/coalesced tick caused a missed vsync and the next
    tick produced a double-sized position jump that was visible as judder.
  - At 8 ms, multiple update() calls are always queued when vsync fires so
    no frames are skipped. Qt coalesces them into a single paint anyway.

ROOT CAUSE ANALYSIS:
  The judder had two initial compounding problems identified in the first pass,
  then two more discovered in the second pass:

  Pass 1 — What was causing it (initial diagnosis):

  Problem 1: Sub-pixel quantization stutter (~2.5 Hz)
    At speed=2, the scroll moved ~0.96 px/frame. floor(0.96) = 0, so the ticker
    stayed locked at the same pixel column for two consecutive frames — then
    jumped 1 pixel. Since 0.96 = 24/25, this repeated every 25 frames = 400 ms
    ≈ 2.5 Hz. Exactly what was seen. Fixed by moving to QPointF drawing so the
    blit could be positioned at fractional-pixel offsets.

  Problem 2: Timer-callback lag + double-pixel catch-up jumps
    Scroll position was computed in update_scroll() at the moment the QTimer
    fired, but paintEvent() ran some milliseconds later. When the event loop was
    briefly busy, a tick fired late — the next tick then had a double-sized
    delta, skipping 2 pixels at once and producing an irregular secondary judder
    on top of the 2.5 Hz pattern. Fixed by moving delta-time computation into
    paintEvent itself, so position is accurate to the actual render moment.

  Pass 2 — Residual judder at higher speeds (second diagnosis):

  Problem 3: QElapsedTimer::elapsed() integer quantization
    elapsed() returns a qint64 integer millisecond count. At ~16 ms frame
    intervals, ±1 ms is a ~6% error per delta — alternating frames computed
    delta_ms=15 and delta_ms=17, creating a slight but consistent advance
    irregularity. Fixed by switching to nsecsElapsed() / 1_000_000.0 for
    sub-microsecond float-ms precision.

  Problem 4: Windows 15.6 ms system clock granularity
    Even with Qt.PreciseTimer, the Windows default multimedia timer resolution
    is 15.625 ms. An 8 ms QTimer would fire at either 0 ms or 15.6 ms — wildly
    inconsistent. Fixed by calling timeBeginPeriod(1) at startup via ctypes
    (winmm.dll, already present) so the system clock ticks every 1 ms, making
    the 8 ms timer fire within ±1 ms of target. OS auto-restores on exit.


Version 2.5.4 (2026-03-25)
--------------------------
Smart polling, seamless ticker tiling, network resilience, WP/LP final display

FEATURES:
+ Smart polling intervals — no longer fetches at a fixed rate regardless of game state
  - Any game In Progress / Live              → normal interval (user setting, default 10 s)
  - Any scheduled game starting within 2 min → normal interval (pre-game urgency window)
  - All games scheduled and >2 min away      → idle mode: poll every 5 minutes
  - All games Final                          → stop update_timer; hand off to next-day mode
  - Interval is re-evaluated after every successful fetch via _reschedule_update_timer()

+ Winning/Losing pitcher line on Final games
  - When a game ends, the live P: / AB: subtext is replaced with:
      WP: LastName ERA W-L   (under the winning team)
      LP: LastName ERA W-L   (under the losing team)
  - Data sourced from liveData.decisions (winner/loser) and boxscore seasonStats
  - Falls back gracefully if decisions are not yet available in the API response

+ Network resilience — ticker stays visible during internet outages
  - Last successful fetch is cached in _cached_games
  - On a network/API failure, cached games are shown unchanged and a small
    amber "DATA DELAYED" badge appears in the top-right corner of the bar
  - When connectivity resumes the badge is automatically removed on next
    successful fetch without requiring a restart
  - First-launch with no connectivity shows the "No MLB games scheduled today"
    message until data can be obtained

FIXES:
* "No MLB games scheduled today" message started mid-screen instead of scrolling in
  from the right edge
  - Fixed by resetting scroll_offset to -window_width after building the no-games
    pixmap, matching the same off-screen-right start used by normal game content

* With only 1–2 games the ticker showed a large blank gap followed by a hard reset
  - Root cause: double-width pixmap strategy only works when total_width ≥ window_width;
    shorter content left visible blank regions and the offset-0 wrap produced a visible jump
  - Fix: build_ticker_pixmap now computes num_copies = max(2, window_width // total_width + 2)
    and tiles that many complete content repetitions so the pixmap always covers
    (window_width + total_width) pixels — the window is fully covered at every scroll position
  - _scroll_max_width is now set inside build_ticker_pixmap (= one total_width period)
    rather than derived from the pixmap's physical width, making the loop period correct
    regardless of how many copies were tiled
  - scroll wrap always returns to 0.0 (seamless); the previous "jump to -window_width"
    workaround for narrow content is removed


Version 2.5.3 (2026-03-20)
--------------------------
Inning indicator overlap fix, Standings row contrast improvement

FIXES:
* Inning indicator text (e.g. "B15") could be clipped by or overlap the home
  team logo in the ticker for extra-inning games
  - Diamond pixmap right gutter widened from +14 px to +30 px, giving 3-char
    inning labels (T10, B12, etc.) room to fully render within the pixmap
  - build_game_pixmap now derives effective_after_diamond from the actual
    inning text right-edge (already stored in logical px on the pixmap), so
    both total_width and the drawing x-advance guarantee a clean 6 px gap
    between the inning label and the home score regardless of DPR
  - Removed an erroneous / self.dpr division on the inning_text_right value
    that was under-counting the needed space on HiDPI displays

IMPROVEMENTS:
* Standings window alternating row backgrounds made more readable
  - Even rows: rgba(255,255,255,12) → rgba(255,255,255,35) — clearly visible
    as dark grey on the near-black window background (~14 % white vs. ~5 %)
  - Odd rows: transparent → rgba(255,255,255,10) — a slight step above pure
    black so each row type is visually distinct without harming dark team name
    readability


Version 2.5.2 (2026-03-20)
--------------------------
AppBar desktop space not freed on exit (Windows 10), dual EXE build

FIXES:
* Reserved desktop space not released on exit — reproducible on Windows 10
  - Extracted ABM_REMOVE into a dedicated remove_appbar() method guarded by
    an _appbar_registered flag (safe against double-calls)
  - Connected app.aboutToQuit signal to remove_appbar() so the reservation is
    always freed before the event loop exits, regardless of how quit was
    triggered (tray menu, sys.exit, etc.)
  - closeEvent still calls remove_appbar() as a secondary path; the flag
    prevents double-removal if aboutToQuit already ran first
  - On Windows 10 closeEvent could arrive after the event loop ended when
    quitting via the tray icon; aboutToQuit fires synchronously first and is
    the reliable guarantee on all Windows versions

BUILD:
* build_performance.bat now builds both EXE targets in one run
  - MLB-TCKR-console.exe — console window visible (for diagnostics)
  - MLB-TCKR.exe          — console window hidden (production)
  - Both spec files (MLB-TCKR-console.spec / MLB-TCKR.spec) were already
    correct; only the batch script was updated
  - Failure detection uses file-existence checks instead of errorlevel so
    PyInstaller warnings do not abort the second build


Version 2.5.1 (2026-03-20)
--------------------------
LED black background fix

FIXES:
* Ticker background rendering as grey instead of the LED black gradient
  - WA_NoSystemBackground was set to False, causing Qt to paint the system
    palette colour (grey) onto the window surface before paintEvent ran;
    corrected to True so only the custom dark gradient is drawn
  - Background cache key now includes widget width (was height-only); prevents
    stale narrower pixmap being reused on resize, which left grey unpainted
    regions on the right side of the ticker


Version 2.5.0 (2026-03-17)
--------------------------
Desktop reservation hardening, Standings overhaul, About dialog, context menu additions,
intro text fix

FEATURES:
+ About dialog — accessible via right-click menu → "About MLB-TCKR..."
  - Displays app name, version, author, copyright, license, clickable website link
  - MLB trademark disclaimer included
  - Same LED-style dark background with white border and scanlines as Standings window
  - Draggable; modal
+ Pause Ticker / Unpause Ticker — added to right-click context menu
  - Label reflects current state (shows "Unpause Ticker" when already paused)
  - Identical behaviour to the P keyboard shortcut
+ Standings: MLB STANDINGS title — window heading updated from "STANDINGS"

IMPROVEMENTS:
* AppBar desktop space reservation hardened
  - Physical height now read via Win32 GetClientRect() instead of int(height * DPR);
    eliminates 1-pixel truncation at certain DPI scaling factors (125 %, 175 % …)
  - Monitor origin and width sourced from GetMonitorInfo() rather than hard-coded (0,0);
    correct on multi-monitor setups where the primary screen is not at the virtual origin
  - ABM_WINDOWPOSCHANGED message sent after setGeometry(); required by the AppBar
    protocol to force the shell to fully update the desktop work-area boundary
    (root cause of the ~85 % reservation issue)
  - Prior top-edge reservations by other programs detected via GetMonitorInfo().rcWork
    and logged; ABM_QUERYPOS automatically slots MLB-TCKR below them without clobbering
* Standings window — not always-on-top; other windows can now be focused normally
  when overlapping the Standings window (Qt.WindowStaysOnTopHint removed)
* Standings outer border changed from green (#00FF44) to white (#FFFFFF)
* Standings header divider now tracks the active league:
  - Red (#FF2222) when American League is selected
  - Blue (#2266FF) when National League is selected
  - Updates immediately on league switch
* Standings team name column widened (160 → 210 logical px at 100 % scale) so that
  long names such as "D-backs" / "Diamondbacks" are never clipped; ideal-width
  constant updated to prevent over-compression on smaller screens
* Intro text gap fixed — "MLB-" and "TCKR" were rendering with a visible inter-word
  gap due to the font's right-side bearing after the hyphen; now draws the two
  segments using tightBoundingRect() for pixel-precise placement

FIXES:
* NameError: _StandingsWorker not defined — class definition header was missing;
  the done signal and run() method were accidentally indented inside StandingsWindow
  instead of their own QThread subclass; extracted into proper class


Version 2.4.0 (2026-03-16)
--------------------------
Right-click context menu, LED/glass visual match, Cython EXE fix, warning suppression

FEATURES:
+ Right-click context menu on ticker bar — same options as the system tray icon
  - Refresh Games / Standings... / Settings... / Quit
  - Shares the same StandingsWindow instance with the tray icon (no duplicate windows)
  - Handled via Qt.CustomContextMenu policy + mousePressEvent for reliability on
    frameless AppBar/Tool windows where contextMenuEvent alone is not always delivered

VISUAL:
* LED background gradient updated to match reference (blue-tinted near-black):
  - Top: rgb(15,18,22) → mid: rgb(10,12,16) → bottom: rgb(8,10,14)
  - Previously was neutral charcoal; now has the steel-blue tone of a real LED board
* Glass overlay updated to match reference:
  - Alpha stops changed from near-invisible (8,3,0,3,6) to (48,12,4,0)  
  - Added 1px top-edge highlight at y=2, alpha 55 — simulates glass catching light
    at the bezel (was absent entirely before)

FIXES:
* Cython extension not loading in PyInstaller EXE — two root causes fixed:
  - numpy.import_array() call was missing from mlb_ticker_utils_cython.pyx; any
    Cython extension that cimports numpy must call this to initialise the C API
  - numpy was in the excludes list of both spec files; removed and added to
    hiddenimports so it is bundled correctly
* build_performance.bat / build_performance_laptop.bat now run PyInstaller
  automatically after compiling the Cython .pyd, so the EXE is always rebuilt
  with the current extension bundled (step labels updated to [1/5]–[5/5])
* RequestsDependencyWarning (urllib3/charset_normalizer version mismatch) suppressed
  at startup in MLB-TCKR.py and in the PyInstaller runtime hook
  pyi_rth_requests_charset.py — purely cosmetic warning; requests works correctly
* ImportError message from failed Cython import now printed in full so the real
  reason is visible instead of the generic fallback message

TECHNICAL:
* mousePressEvent, contextMenuEvent, and _show_context_menu correctly placed inside
  MLBTickerWindow class (previous session had them accidentally inside StandingsWindow)


Version 2.3.0 (2026-03-16)
--------------------------
Performance optimizations, smooth scrolling, FPS overlay, and network/proxy settings

FEATURES:
+ Network Tab in Settings — dedicated tab for proxy and SSL certificate configuration
  - Enable Proxy checkbox + URL field (auto-prefixes http:// if scheme is missing)
  - Use Certificate File checkbox + file path input + Browse button (file dialog)
  - Proxy settings applied immediately on Save without requiring a restart
  - Settings persisted as use_proxy, proxy, use_cert, cert_file in JSON
+ FPS Overlay — "Show FPS Overlay" checkbox in General settings
  - Displays live FPS value (e.g. "61.2 FPS") in bottom-right corner of ticker bar
  - Bright green (#00FF44) text using small_font; subtle dark backing for readability
  - Updated once per second; only rendered when overlay is enabled
+ Delta-time scrolling — scroll rate is now independent of Windows timer jitter
  - Uses QElapsedTimer to measure real elapsed milliseconds between frames
  - Eliminates the ~2x/sec judder caused by Windows 15.6 ms timer resolution

IMPROVEMENTS:
* Home team score gap — guaranteed minimum 6px gap after inning indicator text
  (previously the score could overlap "B15" or other multi-digit inning labels)
* MLB logo caching — mlb.png now cached in MLB_LOGO_CACHE keyed by (size, dpr);
  avoids repeated disk scans and rescaling on every ticker rebuild
* Multi-word player name display — names with 3+ words show last two
  (e.g. "Michael Harris II" → "Harris II")
* Standings window dynamic resize — window and font sizes computed from screen
  geometry and device pixel ratio; never overflows smaller displays
* Ticker bar small_font size increased by 1pt for player names and W-L records
* Font loading consolidated — _CUSTOM_FONT_FAMILY, _RECORD_FONT_FAMILY, and
  _OZONE_FONT_FAMILY are module-level globals; each font loaded at most once

PERFORMANCE:
* Fingerprint-based pixmap rebuild — _games_fingerprint() encodes all visual state
  as a tuple; build_ticker_pixmap() is only called when data actually changes,
  eliminating the brief main-thread stall on each 10-second refresh cycle
* Per-game pixmap cache — _game_pixmap_cache keyed by game_id + fingerprint tuple;
  unchanged games are never repainted on a data refresh
* Background key cached as tuple (led_background, bg_opacity, height) instead of
  dict; avoids per-frame heap allocation in paintEvent
* _scroll_speed_px_per_ms and _scroll_max_width pre-computed when pixmap changes;
  no math in the 60hz scroll path
* QTimer.PreciseTimer type applied to both scroll timer (16ms/60fps) and intro timer

TECHNICAL:
* normalize_proxy_url() — prepends http:// if no scheme prefix present
* apply_proxy_settings() — writes HTTP_PROXY, HTTPS_PROXY, REQUESTS_CA_BUNDLE
  environment variables so requests/statsapi pick them up transparently
* apply_proxy_settings() called at startup (main()) and on every Settings save
* Windows system certificate store injected into requests at startup via
  pip_system_certs.wrapt_requests.inject_truststore(); when no custom cert file
  is configured, all SSL verification uses the system trust store automatically;
  degrades gracefully if pip_system_certs is not installed
* draw_baseball_diamond() stores pixmap._inning_text_right_phys attribute so
  build_game_pixmap() can compute the exact home-score x position


Version 2.2.0 (2026-03-16)
--------------------------
Standings window, keyboard shortcuts, font preview, DPI-aware AppBar, custom defaults

FEATURES:
+ Standings window — full AL/NL standings popup with LED-style background
  - Split header: "STANDINGS" title (white) above "American League | National League" row
  - American League label glows bright red when active; National League glows bright blue
  - Clicking either league label switches the displayed standings instantly
  - Three fixed-width division columns (East / Central / West) with table alignment
  - Per-column headers: logo spacer + Team / W-L / Pct. / L10
  - Team logos, team-color names, and stat cells with consistent 40px row height
  - Alternating row shading; bright green border and divider
  - Background fetch via _StandingsWorker QThread (non-blocking)
  - Draggable, frameless, always-on-top window; centered on open and after data loads
  - "Standings..." entry added to system tray menu
+ Keyboard shortcuts (when ticker window has focus):
  - S / s   → open Standings window (or raise if already visible)
  - .       → open Settings dialog
  - R / r   → force data refresh
  - P / p   → pause / unpause scroll
  - Q / q   → quit application completely
+ Font preview delegate — font combo box renders each family name in its own typeface
+ DPI-aware AppBar — correctly reserves desktop space at 125 %, 150 %, 200 % display scaling
+ Intro animation — pixel-reveal (block randomize-in / hold / block randomize-out) using
  Ozone-xRRO font and mlb-reverse.png logo at physical resolution

FIXES:
* AppBar called after self.show() so HWND is valid and visible
* ABM_QUERYPOS step added (was missing) so Windows can negotiate bar position
* AppBar physical/logical pixel conversion: Win32 receives physical pixels, Qt setGeometry
  receives logical pixels (divided by devicePixelRatio)

IMPROVEMENTS:
* Default font changed to Ozone, font scale to 150 %, ticker height to 64 px, speed to 5
* Default background_opacity changed to 255 (fully opaque)
* show_team_cities defaults to False (nickname-only mode)
* Nine team colors baked into MLB_TEAM_COLORS_DEFAULT reflecting user preferences
* StandingsWindow uses fixed pixel widths per cell (_W_LOGO=44, _W_NAME=160, _W_WL/PCT/L10=90)
  instead of stretch ratios — guarantees perfect column alignment
* All standings font sizes doubled (header 64 px, league row 40 px, div titles 36 px,
  col headers 26 px, team names 30 px, stats 26 px)

TECHNICAL:
* _AL_DIVISIONS / _NL_DIVISIONS dicts map division names to MLB team IDs
* fetch_standings() uses statsapi.get('standings', ...) with hydration; parses lastTen
  from splitRecords; sorts best→worst by (-wins, losses)
* StandingsWindow._populate() removes stale row widgets before re-adding (prevents duplication)
* FontPreviewDelegate.paint() renders family + "  —  AaBbCc 123" suffix at 18px
* scroll_paused and is_hovered state variables drive enterEvent/leaveEvent/keyPressEvent logic


Version 2.1.0 (2026-03-15)
--------------------------
HiDPI rendering fixes, font selection, and packaging support

FEATURES:
+ PyInstaller spec file (MLB-TCKR.spec) for single-file EXE distribution
+ Font selection in Settings now applies immediately (no longer locked to LED Board-7)
+ register_all_font_files() scans app directories and registers all .ttf/.otf files at startup
+ Font preview in Settings dialog updates live as font is changed
+ sys._MEIPASS support in register_all_font_files() for frozen (PyInstaller) builds

FIXES:
* Fixed font appearing compressed/blurry at Windows display scales above 100%
* Fixed PIRATES/large team name text appearing ghosted vs. smaller fallback text
* Fixed @ symbol positioned too far left with extra empty space to its right
* Fixed only one game visible when scrolling (x_offset not advancing in draw loop)
* Fixed DPR mismatch: x_offset was incrementing by physical pixels in a logical coordinate space

IMPROVEMENTS:
* AA_EnableHighDpiScaling and AA_UseHighDpiPixmaps set before QApplication creation
* All off-screen pixmaps now created at physical resolution (logical × DPR) with setDevicePixelRatio()
* TextAntialiasing disabled on game pixmap painter for crisp LED font dot-grid
* draw_baseball_diamond() accepts dpr parameter for native-resolution rendering
* update_scroll() and max_width calculation use logical pixel range (width / dpr / 2)
* @ symbol spacing symmetric: 15px gap on both sides for centered appearance
* total_width calculations in build_game_pixmap() aligned with actual draw loop advances

TECHNICAL:
* self.dpr = QApplication.primaryScreen().devicePixelRatio() stored at init
* build_game_pixmap() pixmaps: QPixmap(int(w * dpr), int(h * dpr)) + setDevicePixelRatio(dpr)
* build_ticker_pixmap() loop: logical_width = int(pixmap.width() / dpr) for x_offset math
* MLB-TCKR.spec: auto-detects cpXYY-win_amd64.pyd matching current Python version
* MLB-TCKR.spec: bundles led_board-7.ttf, SubwayTicker.ttf, mlb.png; excludes numpy/pandas/numba


Version 2.0.0 (2026-03-14)
--------------------------
Major performance and efficiency update

FEATURES:
+ Intelligent polling system - automatically stops frequent updates when all games finish
+ Hourly next-day checks after games finish to detect midnight rollover
+ Automatic resume of normal polling when new day's games detected
+ Background threading (GameDataWorker QThread) - prevents scroll interruptions during API calls
+ Hover-to-pause functionality - ticker pauses when mouse hovers over it
+ 60 FPS rendering with PreciseTimer (16ms frame time)
+ Sub-pixel smooth scrolling using float positions
+ Cached background and overlay rendering for better performance
+ Hardware acceleration with SmoothPixmapTransform
+ Optional Cython optimization module with Python fallback
+ Build automation script (build_performance.bat) for Cython compilation

IMPROVEMENTS:
* Optimized all spacing throughout ticker (2-20px between elements)
* Centered inning indicator with baseball diamond bases
* Adjusted outs circles positioning (moved down 2 pixels)
* Enhanced closeEvent to properly clean up both timers and worker threads
* Date tracking for automatic day transitions
* Improved API usage efficiency (drops from ~360 calls/hour to 1 call/hour after games finish)

TECHNICAL:
* GameDataWorker class for non-blocking API calls
* Dual-timer system: normal (10s) and next-day (3600s) polling
* check_all_games_finished() method for game status detection
* check_for_next_day_games() for midnight rollover detection
* Console logging for polling state changes


Version 1.5.0 (2026-03-13)
--------------------------
Visual polish and layout refinements

FEATURES:
+ LED-style gradient background (20,20,20→0,0,0→20,20,20)
+ Glass overlay effect with transparency controls
+ Adjustable background opacity (0-255)
+ Team Colors tab in Settings dialog for custom color overrides
+ Settings persistence in %APPDATA%\MLB-TCKR\MLB-TCKR.Settings.json

IMPROVEMENTS:
* Finalized layout: TEAM LOGO SCORE | DIAMOND | SCORE LOGO TEAM format
* Capitalized all team names (YANKEES, DODGERS, etc.)
* Final games show "F" indicator using same layout as live games
* Gold inning indicator (#FFD700, 13pt bold)
* Optimized spacing between all elements
* Enhanced visual consistency across game states


Version 1.0.0 (2026-03-12)
--------------------------
Initial release

FEATURES:
+ MLB-StatsAPI integration for live game data
+ Scrolling ticker bar with team logos and scores
+ Baseball diamond visualization (3 triangle bases, 3 circular outs)
+ Live runner detection (bright green #00FF00)
+ Live outs display (bright red #FF0000)
+ Inning indicator (Top/Bottom format: T5, B5)
+ 30 MLB team official colors with defaults
+ Team logo support (PNG files in %APPDATA%\MLB-TCKR\MLB-TCKR.images\)
+ Windows AppBar integration for persistent top-of-screen docking
+ System tray icon with context menu
+ Settings dialog with General tab
+ Custom LED Board-7 font support with Arial fallback
+ Configurable scroll speed and update intervals
+ Toggle options for team records, cities, final games, scheduled games
+ Automatic data refresh every 10 seconds (configurable)

TECHNICAL:
* PyQt5 GUI framework
* FramelessWindowHint and TranslucentBackground
* Windows AppBar API integration (ABM_NEW, ABM_SETPOS, ABE_TOP)
* Live game feed parsing from linescore.offense data
* Team nickname detection and normalization
* Logo file case-insensitive search
* Fallback rendering for missing logos


Build Information
-----------------
Python Version: 3.13
Required Python Path: C:\Users\prc\AppData\Local\Programs\Python\Python313\python.exe
Build Script: build_performance.bat

Required Dependencies:
- statsapi (MLB data)
- PyQt5 (GUI framework)
- Cython (optional, for optimizations)

Performance Files:
- mlb_ticker_utils_cython.pyx (Cython optimizations)
- setup_mlb_cython.py (Cython build configuration)
- build_performance.bat (Windows build automation)
- PERFORMANCE.md (optimization documentation)


Known Issues
------------
None reported


Future Enhancements
-------------------
Potential features for consideration:
- Game alerts and notifications
- Score change notifications
- Expanded statistics display
- Multi-monitor support
- Custom positioning options
- Additional visual themes
