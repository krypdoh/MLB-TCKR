Plan to increase spacing between games on the ticker by 50%.

Steps:
1. Locate the `build_ticker_pixmap` method in MLB-TCKR.py.
2. Find the line initializing the `spacing` variable (currently set to 67).
3. Change the value from 67 to 101 (67 * 1.5 = 100.5, rounded up to 101).
4. Save the file.

This will increase the logical pixel spacing between each game's pixmap by 50%.